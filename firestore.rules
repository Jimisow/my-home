rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Seuls les utilisateurs authentifies (le compte foyer) peuvent lire/ecrire
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
